{
  "name": "auto_versioning",
  "version": "1.0.0",
  "description": "This shell script automates the process of bumping a version number in json formatted version file following semver standard (major, minor, patch).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "conventional-changelog": "^3.0.5",
    "conventional-recommended-bump": "^4.0.4"
  }
}
